# Claude Code harness internals — session-specific, never project content
.claude/projects/
.claude/scheduled_tasks.lock

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/

# Secrets — never commit real credentials (course FAKE placeholders are fine)
.env
.env.*
!.env.example
*.pem
*.key

# Node / build artifacts (root + packages/*, not just web/)
node_modules/
.next/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# Editor extension binaries
*.vsix
